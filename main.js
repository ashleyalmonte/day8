  // SCOPES

  // let
  // var
  // const
  // no keyword -- GLobal variable

  let str = "Welcome to class"

  function useless() {
    let str = 'be a useless function'
    console.log(str)

  }
    function speak() {
    console.log(str);
    if (condition == true) {
   }

  function calcTaxNY(Price) {
    let tax = 1000000
    console.log(price + tax)
  }
  function calTaxXs(price) {
    let tax = 3
    console.log(price + tax)
  }

  calctaxNY()
  useless()
  speak()
